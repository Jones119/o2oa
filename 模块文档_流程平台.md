# 流程平台模块详细文档

## 模块概述

流程平台模块是O2OA的核心业务模块，提供完整的工作流引擎、流程设计、表单设计、任务管理等功能。

### 模块组成

| 模块名 | 类型 | 说明 |
|-------|------|------|
| x_processplatform_core_entity | 实体模块 | 流程相关JPA实体 |
| x_processplatform_core_express | 接口模块 | 流程服务调用接口 |
| x_processplatform_assemble_control | 控制模块 | 流程管理API |
| x_processplatform_assemble_designer | 设计器模块 | 流程设计器API |
| x_processplatform_assemble_surface | 展示模块 | 任务中心API |
| x_processplatform_assemble_bam | BAM模块 | 流程监控分析 |
| x_processplatform_service_processing | 服务模块 | 流程引擎服务 |

---

## 实体定义 (x_processplatform_core_entity)

### 设计类实体

#### 1. Application (流程应用)

**路径**: `com.x.processplatform.core.entity.element.Application`

**主要属性**:
- `id`: 唯一标识
- `name`: 应用名称
- `alias`: 别名
- `description`: 描述
- `icon`: 图标

**关系**: 包含多个Process、Form、Script等

#### 2. Process (流程定义)

**路径**: `com.x.processplatform.core.entity.element.Process`

**主要属性**:
- `id`: 唯一标识
- `name`: 流程名称
- `alias`: 别名
- `application`: 所属应用
- `startForm`: 启动表单
- `startMode`: 启动模式

#### 3. Activity (活动节点)

**路径**: `com.x.processplatform.core.entity.element.Activity`

**活动类型**:
- `Begin`: 开始节点
- `End`: 结束节点
- `Manual`: 人工节点
- `Service`: 服务节点
- `Script`: 脚本节点
- `Agent`: 代理节点
- `Delay`: 延迟节点
- `Parallel`: 并行节点
- `Merge`: 合并节点
- `Embed`: 嵌入流程
- `Choice`: 选择路由
- `Cancel`: 取消操作
- `Invoke`: 调用操作
- `Publish`: 发布文档

#### 4. Route (路由)

**路径**: `com.x.processplatform.core.entity.element.Route`

**主要属性**:
- `id`: 唯一标识
- `name`: 路由名称
- `fromActivity`: 源活动
- `toActivity`: 目标活动
- `passOrder`: 通过顺序
- `condition`: 条件表达式

#### 5. Form (表单)

**路径**: `com.x.processplatform.core.entity.element.Form`

**主要属性**:
- `id`: 唯一标识
- `name`: 表单名称
- `alias`: 别名
- `application`: 所属应用
- `mobileForm`: 移动端表单
- `formData`: 表单数据 (JSON)

#### 6. Script (脚本)

**路径**: `com.x.processplatform.core.entity.element.Script`

**主要属性**:
- `id`: 唯一标识
- `name`: 脚本名称
- `alias`: 别名
- `application`: 所属应用
- `scriptType`: 脚本类型
- `text`: 脚本内容

#### 7. Agent (代理)

**路径**: `com.x.processplatform.core.entity.element.Agent`

**主要属性**:
- `id`: 唯一标识
- `name`: 代理名称
- `alias`: 别名
- `application`: 所属应用
- `cron`: 定时表达式
- `text`: 脚本内容

### 运行时实体

#### 8. Work (工作实例)

**路径**: `com.x.processplatform.core.entity.content.Work`

**主要属性**:
- `id`: 唯一标识
- `title`: 标题
- `application`: 应用
- `process`: 流程
- `activity`: 当前活动
- `workCreateType`: 创建类型
- `creatorPerson`: 创建人
- `startTime`: 开始时间
- `properties`: 属性数据 (JSON)
- `data`: 业务数据 (JSON)

**状态**:
- `start`: 启动中
- `processing`: 处理中
- `hanging`: 挂起
- `completed`: 完成
- `terminated`: 终止

#### 9. WorkCompleted (已完成工作)

**路径**: `com.x.processplatform.core.entity.content.WorkCompleted`

**说明**: Work完成后的归档记录

#### 10. Task (待办任务)

**路径**: `com.x.processplatform.core.entity.content.Task`

**主要属性**:
- `id`: 唯一标识
- `work`: 所属工作
- `title`: 标题
- `person`: 处理人
- `identity`: 处理身份
- `activity`: 活动
- `form`: 表单
- `startTime`: 开始时间
- `expiredTime`: 过期时间
- `processMode`: 处理模式

#### 11. TaskCompleted (已办任务)

**路径**: `com.x.processplatform.core.entity.content.TaskCompleted`

**说明**: Task完成后的归档记录

#### 12. Read (待阅)

**路径**: `com.x.processplatform.core.entity.content.Read`

**说明**: 需要阅读的记录

#### 13. ReadCompleted (已阅)

**路径**: `com.x.processplatform.core.entity.content.ReadCompleted`

**说明**: Read完成后的归档记录

#### 14. Review (参阅)

**路径**: `com.x.processplatform.core.entity.content.Review`

**说明**: 可参阅的记录

#### 15. Attachment (附件)

**路径**: `com.x.processplatform.core.entity.content.Attachment`

**主要属性**:
- `id`: 唯一标识
- `work`: 所属工作
- `name`: 文件名
- `extension`: 扩展名
- `size`: 大小
- `storage`: 存储位置

#### 16. WorkLog (工作日志)

**路径**: `com.x.processplatform.core.entity.content.WorkLog`

**说明**: 记录工作流转的详细日志

#### 17. SerialNumber (序列号)

**路径**: `com.x.processplatform.core.entity.content.SerialNumber`

**说明**: 生成流程实例编号

---

## 流程引擎核心 (x_processplatform_service_processing)

### 工作原理

流程引擎采用事件驱动+状态机模式：

```
1. 接收请求(API/定时/事件)
2. 创建Work实例
3. 到达Begin活动
4. 执行Activity逻辑
5. 计算Route
6. 流转至下一Activity
7. 创建Task/Read/Review
8. 等待用户处理
9. 继续流转直到End
```

### 核心处理类

| 类名 | 功能 |
|-----|------|
| ProcessProcessing | 流程处理引擎 |
| ActivityProcessing | 活动处理基类 |
| BeginProcessing | 开始节点处理 |
| ManualProcessing | 人工节点处理 |
| ServiceProcessing | 服务节点处理 |
| ScriptProcessing | 脚本节点处理 |
| DelayProcessing | 延迟节点处理 |
| ParallelProcessing | 并行节点处理 |
| MergeProcessing | 合并节点处理 |
| EndProcessing | 结束节点处理 |
| RouteProcessing | 路由计算处理 |
| TaskCreateProcessing | 任务创建处理 |

### 人工节点处理流程

```
1. 到达Manual活动
2. 计算处理人(Identity矩阵)
3. 创建Task
4. 发送消息通知
5. 等待用户处理
6. 用户提交
7. 计算路由
8. 流转至下一活动
```

### 处理人计算 (ManualTaskIdentityMatrix)

支持多种处理人配置方式：

1. **指定人员**: 直接选择Person
2. **指定组织**: 选择Unit下的人员
3. **指定身份**: 选择Identity
4. **指定群组**: 选择Group
5. **指定角色**: 选择Role
6. **指定职务**: 选择UnitDuty
7. **脚本计算**: 通过脚本动态计算
8. **上级组织**: 计算上级组织人员
9. **流程发起人**: 取creatorPerson
10. **之前活动处理人**: 取previous处理人

---

## 控制层模块 (x_processplatform_assemble_surface)

### 主要API

#### 任务中心API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/task/list/person | 获取我的待办 |
| GET | /jaxrs/taskcompleted/list/person | 获取我的已办 |
| GET | /jaxrs/read/list/person | 获取我的待阅 |
| GET | /jaxrs/readcompleted/list/person | 获取我的已阅 |
| GET | /jaxrs/review/list/person | 获取我的参阅 |
| GET | /jaxrs/draft/list/person | 获取我的草稿 |

#### 工作处理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /jaxrs/work/process | 处理工作 |
| POST | /jaxrs/work/reset | 重置工作 |
| POST | /jaxrs/work/retract | 撤回工作 |
| POST | /jaxrs/work/rollback | 回滚工作 |
| POST | /jaxrs/work/split | 拆分工作 |
| POST | /jaxrs/work/delete | 删除工作 |

#### 启动流程API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/process/list/application | 获取流程列表 |
| POST | /jaxrs/work/process/{flag} | 启动流程 |

---

## 设计器模块 (x_processplatform_assemble_designer)

### 流程设计器功能

1. **流程设计**: 拖拽式流程设计
2. **表单设计**: 可视化表单设计器
3. **脚本管理**: 脚本编写与管理
4. **应用管理**: 应用导入导出

### 设计器API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/application | 获取应用列表 |
| POST | /jaxrs/application | 创建应用 |
| GET | /jaxrs/application/{flag} | 获取应用 |
| GET | /jaxrs/application/{flag}/process/list | 获取流程列表 |
| POST | /jaxrs/process | 创建流程 |
| PUT | /jaxrs/process/{flag} | 更新流程 |
| GET | /jaxrs/process/{flag} | 获取流程 |
| POST | /jaxrs/form | 创建表单 |
| PUT | /jaxrs/form/{flag} | 更新表单 |

---

## 前端组件

### 流程设计相关组件

| 组件 | 路径 | 功能 |
|-----|------|------|
| ProcessDesigner | x_component_process_ProcessDesigner | 流程设计器 |
| FormDesigner | x_component_process_FormDesigner | 表单设计器 |
| ApplicationExplorer | x_component_process_ApplicationExplorer | 应用管理 |
| ProcessManager | x_component_process_ProcessManager | 流程管理 |

### 任务中心组件

| 组件 | 路径 | 功能 |
|-----|------|------|
| TaskCenter | x_component_process_TaskCenter | 任务中心 |
| Work | x_component_process_Work | 工作处理 |
| WorkMobile | x_component_process_WorkMobile | 移动端工作处理 |

### 表单组件 (x_component_process_Xform)

提供大量表单控件：

| 控件 | 说明 |
|-----|------|
| Textfield | 文本框 |
| Textarea | 多行文本 |
| Number | 数字 |
| Currency | 货币 |
| Date | 日期 |
| Datetime | 日期时间 |
| Select | 下拉选择 |
| Radio | 单选框 |
| Checkbox | 复选框 |
| Org | 组织选择 |
| Person | 人员选择 |
| Identity | 身份选择 |
| Attachment | 附件 |
| Office | Office文档 |
| PdfView | PDF查看 |
| DataTable | 数据表格 |
| ImageClipper | 图片裁剪 |

---

## 脚本开发

### 可用脚本位置

1. **流程脚本**: Process中的Script活动
2. **表单脚本**: Form中的事件脚本
3. **路由脚本**: Route中的条件判断脚本
4. **Agent脚本**: 定时执行的Agent

### 脚本上下文对象

在脚本中可使用以下对象：

| 对象 | 说明 |
|-----|------|
| `this.work` | 当前Work对象 |
| `this.data` | 业务数据对象 |
| `this.organization` | 组织服务 |
| `this.processPlatform` | 流程服务 |
| `this.query` | 查询服务 |
| `this.log` | 日志对象 |
| `this.context` | 上下文 |

### 脚本示例

```javascript
// 路由条件判断脚本
var amount = this.data.get("amount");
if (amount > 10000) {
    return true; // 金额大于10000走此路由
}
return false;

// 获取部门经理
var unit = this.organization.identity().getObject(this.work.getIdentity()).getUnit();
var manager = this.organization.unit().getObject(unit).getManagerList();

// 创建新工作
var newWork = this.processPlatform.createWork(
    "applicationId", 
    "processId", 
    "title", 
    {key: "value"}
);
```

---

## 工作流设计模式

### 1. 串行流程

```
Begin → Manual1 → Manual2 → Manual3 → End
```

### 2. 并行流程

```
Begin → Parallel → ManualA
                 ↘ ManualB
              → Merge → End
```

### 3. 选择流程

```
Begin → Choice → Route1 → ManualA → End
             ↘ Route2 → ManualB ↗
```

### 4. 子流程

```
Begin → Manual1 → Embed(子流程) → Manual2 → End
```

### 5. 循环流程

```
Begin → Manual1 → Choice → Route1(通过) → End
                      ↘ Route2(驳回) ↗ Manual1
```

---

## 流程监控 (x_processplatform_assemble_bam)

### 功能

1. 流程实例统计
2. 活动耗时分析
3. 人员效率分析
4. 流程异常监控

### API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/application/{flag}/statistic | 应用统计 |
| GET | /jaxrs/process/{flag}/statistic | 流程统计 |
| GET | /jaxrs/work/statistic/person | 个人统计 |

---

## 配置说明

在 `config/processPlatform.json` 中:

```json
{
  "defaultExpiredHours": 72,
  "workLogEnable": true,
  "triggerConsumeInterval": 10,
  "invokeRetryTimes": 3,
  "publishCmsEnable": false,
  "autoArchiveDays": 90
}
```

---

## 扩展点

1. **自定义活动**: 扩展Activity和ActivityProcessing
2. **自定义表单控件**: 前端组件扩展
3. **流程监听器**: 实现流程事件监听
4. **任务分派策略**: 自定义处理人计算逻辑
5. **消息通知**: 自定义消息发送

---

## 使用示例

### 启动流程

```java
// 通过Express启动流程
Work work = Express.processPlatform().startWork(
    applicationId,
    processId,
    title,
    dataMap,
    identity
);
```

### 处理任务

```javascript
// 前端处理任务
o2.api.processAction.process({
    id: workId,
    routeName: "同意",
    opinion: "同意",
    data: { ... }
}, function(json){
    console.log("处理成功");
});
```

---

## 总结

流程平台模块是O2OA的核心业务模块，提供了强大的工作流引擎和丰富的设计工具。通过可视化设计器可以快速定义复杂的业务流程，通过脚本可以灵活扩展流程逻辑。
