# 组织管理模块详细文档

## 模块概述

组织管理模块是O2OA平台的核心基础模块，负责人员、组织、角色、权限等核心数据的管理。

### 模块组成

组织管理模块由以下子模块组成：

| 模块名 | 类型 | 说明 |
|-------|------|------|
| x_organization_core_entity | 实体模块 | 定义组织相关的JPA实体 |
| x_organization_core_express | 接口模块 | 提供给其他模块调用的接口 |
| x_organization_assemble_control | 控制模块 | 组织管理后台API |
| x_organization_assemble_authentication | 认证模块 | 用户认证相关 |
| x_organization_assemble_express | 展示模块 | 组织数据查询API |
| x_organization_assemble_personal | 个人模块 | 个人中心相关API |

---

## 实体定义 (x_organization_core_entity)

### 主要实体类

#### 1. Person (人员)

**路径**: `com.x.organization.core.entity.Person`

**主要属性**:
- `id`: 唯一标识
- `name`: 姓名
- `unique`: 唯一名称 (登录名)
- `password`: 密码
- `mobile`: 手机号
- `mail`: 邮箱
- `genderType`: 性别
- `status`: 状态
- `icon`: 头像

**相关操作**:
- 创建/编辑/删除人员
- 密码修改
- 人员状态管理

#### 2. Unit (组织)

**路径**: `com.x.organization.core.entity.Unit`

**主要属性**:
- `id`: 唯一标识
- `name`: 组织名称
- `unique`: 唯一名称
- `superior`: 上级组织
- `level`: 组织层级
- `type`: 组织类型

**层级关系**:
支持多级组织树结构，通过superior属性关联

#### 3. Group (群组)

**路径**: `com.x.organization.core.entity.Group`

**主要属性**:
- `id`: 唯一标识
- `name`: 群组名称
- `unique`: 唯一名称
- `personList`: 成员列表

#### 4. Role (角色)

**路径**: `com.x.organization.core.entity.Role`

**主要属性**:
- `id`: 唯一标识
- `name`: 角色名称
- `unique`: 唯一名称
- `personList`: 人员列表
- `groupList`: 群组列表

#### 5. Identity (身份)

**路径**: `com.x.organization.core.entity.Identity`

**主要属性**:
- `id`: 唯一标识
- `name`: 身份名称
- `person`: 所属人员
- `unit`: 所属组织
- `unitDutyList`: 职务列表

#### 6. UnitDuty (组织职务)

**路径**: `com.x.organization.core.entity.UnitDuty`

**主要属性**:
- `id`: 唯一标识
- `name`: 职务名称
- `unit`: 所属组织
- `identityList`: 身份列表

#### 7. Empower (授权)

**路径**: `com.x.organization.core.entity.accredit.Empower`

**主要属性**:
- `id`: 唯一标识
- `fromPerson`: 授权人
- `toPerson`: 被授权人
- `application`: 应用
- `process`: 流程
- `startTime`: 开始时间
- `endTime`: 结束时间

---

## 控制层模块 (x_organization_assemble_control)

### 模块结构

```
x_organization_assemble_control/
├── src/main/java/com/x/organization/assemble/control/
│   ├── ThisApplication.java              # 应用初始化
│   ├── Business.java                     # 业务逻辑
│   ├── AbstractFactory.java              # 工厂基类
│   ├── factory/                          # 数据访问工厂
│   │   ├── PersonFactory.java
│   │   ├── UnitFactory.java
│   │   ├── GroupFactory.java
│   │   ├── RoleFactory.java
│   │   ├── IdentityFactory.java
│   │   ├── UnitDutyFactory.java
│   │   └── PermissionSettingFactory.java
│   └── jaxrs/                            # REST API
│       ├── ActionApplication.java
│       ├── person/                       # 人员API
│       ├── unit/                         # 组织API
│       ├── group/                        # 群组API
│       ├── role/                         # 角色API
│       ├── identity/                     # 身份API
│       └── unitduty/                     # 职务API
└── pom.xml
```

### 核心类说明

#### 1. ThisApplication

应用初始化类，负责模块启动和销毁：

```java
public class ThisApplication {
    protected static Context context;
    
    public static void init() {
        try {
            CacheManager.init(context.clazz().getSimpleName());
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
    
    public static void destroy() {
        try {
            CacheManager.shutdown();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
```

#### 2. Business

业务逻辑聚合类，提供对各Factory的访问

#### 3. *Factory

各实体的数据访问工厂类，继承自AbstractFactory

### REST API 说明

#### 人员管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/person | 获取人员列表 |
| GET | /jaxrs/person/{flag} | 获取单个人员 |
| POST | /jaxrs/person | 创建人员 |
| PUT | /jaxrs/person/{flag} | 更新人员 |
| DELETE | /jaxrs/person/{flag} | 删除人员 |
| PUT | /jaxrs/person/{flag}/password | 修改密码 |

#### 组织管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/unit | 获取组织列表 |
| GET | /jaxrs/unit/{flag} | 获取单个组织 |
| GET | /jaxrs/unit/{flag}/sub | 获取下级组织 |
| GET | /jaxrs/unit/{flag}/sup | 获取上级组织 |
| POST | /jaxrs/unit | 创建组织 |
| PUT | /jaxrs/unit/{flag} | 更新组织 |
| DELETE | /jaxrs/unit/{flag} | 删除组织 |

#### 群组管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/group | 获取群组列表 |
| POST | /jaxrs/group | 创建群组 |
| PUT | /jaxrs/group/{flag} | 更新群组 |
| DELETE | /jaxrs/group/{flag} | 删除群组 |

#### 角色管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/role | 获取角色列表 |
| POST | /jaxrs/role | 创建角色 |
| PUT | /jaxrs/role/{flag} | 更新角色 |
| DELETE | /jaxrs/role/{flag} | 删除角色 |

#### 身份管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/identity | 获取身份列表 |
| GET | /jaxrs/identity/person/{person} | 获取人员的身份 |
| POST | /jaxrs/identity | 创建身份 |
| PUT | /jaxrs/identity/{flag} | 更新身份 |
| DELETE | /jaxrs/identity/{flag} | 删除身份 |

#### 职务管理API

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /jaxrs/unitduty | 获取职务列表 |
| GET | /jaxrs/unitduty/unit/{unit} | 获取组织的职务 |
| POST | /jaxrs/unitduty | 创建职务 |
| PUT | /jaxrs/unitduty/{flag} | 更新职务 |
| DELETE | /jaxrs/unitduty/{flag} | 删除职务 |

---

## 认证模块 (x_organization_assemble_authentication)

### 主要功能

- 用户登录认证
- Token生成与验证
- 密码加密
- 会话管理

### 核心API

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /jaxrs/authentication | 用户登录 |
| DELETE | /jaxrs/authentication | 用户退出 |
| GET | /jaxrs/authentication | 验证Token |
| GET | /jaxrs/captcha | 获取验证码 |

---

## 接口调用模块 (x_organization_core_express)

### 用途

提供给其他模块调用组织服务的接口封装

### 主要接口类

- `Express`: 主要接口入口
- `ExpressAction`: 动作接口
- 各实体的Action类

---

## 依赖关系

### Maven依赖

```xml
<dependencies>
    <dependency>
        <groupId>net.o2oa</groupId>
        <artifactId>x_base_core_project</artifactId>
    </dependency>
    <dependency>
        <groupId>net.o2oa</groupId>
        <artifactId>x_organization_core_entity</artifactId>
    </dependency>
    <dependency>
        <groupId>net.o2oa</groupId>
        <artifactId>x_general_core_entity</artifactId>
    </dependency>
</dependencies>
```

---

## 工作流程

### 1. 用户登录流程

```
1. 前端提交用户名密码
2. AuthenticationAction接收请求
3. 验证验证码(如启用)
4. 查询Person实体验证密码
5. 生成Token
6. 返回Token和用户信息
7. Token存入缓存
```

### 2. 组织查询流程

```
1. 接收查询请求
2. UnitFactory执行查询
3. 根据superior关系构建组织树
4. 返回层级数据
```

### 3. 权限判断流程

```
1. 获取当前用户身份
2. 查询用户角色
3. 查询用户所属群组
4. 查询用户组织职务
5. 综合判断权限
```

---

## 缓存策略

组织模块使用缓存提高性能：

- **人员缓存**: 缓存Person信息
- **组织缓存**: 缓存Unit信息和层级关系
- **角色缓存**: 缓存Role及成员关系
- **权限缓存**: 缓存权限判断结果

---

## 前端组件 (x_component_org)

### 主要功能组件

| 组件 | 功能 |
|-----|------|
| PersonExplorer | 人员管理 |
| UnitExplorer | 组织管理 |
| GroupExplorer | 群组管理 |
| RoleExplorer | 角色管理 |
| Main | 主界面 |

### 组件位置

`o2web/source/x_component_org/`

---

## 配置说明

### 相关配置项

在 `config/person.json` 中:

```json
{
  "initialManagerPassword": "初始管理员密码",
  "tokenExpiredMinutes": 480,
  "captchaEnable": false,
  "passwordComplexityCheck": false
}
```

---

## 扩展点

1. **自定义认证方式**: 扩展AuthenticationAction
2. **自定义人员属性**: 扩展Person实体和PersonAttribute
3. **同步外部组织**: 实现组织同步服务
4. **自定义权限逻辑**: 扩展权限判断Factory

---

## 使用示例

### 通过Express调用组织服务

```java
// 获取人员
Person person = Express.person().getObject(flag);

// 获取组织下的人员
List<Person> persons = Express.person().listWithUnitSubDirect(unitId);

// 获取用户身份
List<Identity> identities = Express.identity().listWithPerson(personId);

// 检查角色
boolean hasRole = Express.role().hasRole(roleName, personId);
```

### REST API调用示例

```javascript
// 获取人员列表
o2.api.orgAction.listPerson(function(json){
    console.log(json.data);
});

// 创建人员
o2.api.orgAction.createPerson({
    name: "张三",
    unique: "zhangsan",
    password: "123456",
    mobile: "13800138000"
}, function(json){
    console.log(json.data);
});
```

---

## 总结

组织管理模块是整个O2OA平台的基础，提供了完整的人员组织架构管理功能。其他业务模块如流程、CMS等都依赖组织模块提供的人员、角色、权限信息。
