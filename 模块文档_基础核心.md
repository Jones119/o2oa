# 基础核心模块文档

## x_base_core_project 模块概述

x_base_core_project 是整个 O2OA 平台的基石，提供了以下核心能力：

1. **ORM/JPA 支持**: JPA 实体基类和工具
2. **缓存系统**: 分布式缓存支持
3. **配置管理**: 统一配置框架
4. **脚本引擎**: GraalVM 脚本执行
5. **工具类库**: 常用工具函数
6. **异常体系**: 统一异常处理
7. **日志系统**: 日志封装
8. **HTTP 客户端**: REST 调用工具
9. **构建工具**: Maven 构建辅助

---

## 核心实体体系

### JpaObject 基类

**路径**: `com.x.base.core.entity.JpaObject`

所有业务实体的基类，提供以下功能：

**主要属性**:
- `id`: 主键 (UUID)
- `distributeFactor`: 分布因子 (用于分片)
- `createTime`: 创建时间
- `updateTime`: 更新时间
- `sequence`: 序列号

**核心注解**:
- `@ContainerEntity`: 标识这是容器管理的实体
- `@CheckPersist`: 持久化前检查
- `@CheckRemove`: 删除前检查

### SliceJpaObject

支持分片的实体基类，继承自 JpaObject。

### DynamicEntity/ DynamicBaseEntity

动态实体支持，运行时创建实体。

---

## 实体管理容器 (EntityManagerContainer)

### EntityManagerContainer

**路径**: `com.x.base.core.container.EntityManagerContainer`

JPA 实体管理器容器，封装了 EntityManager 的操作。

**主要功能**:
- begin(): 开始事务
- commit(): 提交事务
- rollback(): 回滚事务
- get(): 获取实体
- list(): 查询列表
- persist(): 持久化
- remove(): 删除
- update(): 更新

### EntityManagerContainerFactory

创建 EntityManagerContainer 的工厂类。

### SliceEntityManagerContainerFactory

支持分片的实体管理器工厂。

---

## 缓存系统

### CacheManager

**路径**: `com.x.base.core.project.cache.CacheManager`

缓存管理器，提供统一的缓存接口。

**主要功能**:
- init(): 初始化缓存
- put(): 写入缓存
- get(): 读取缓存
- remove(): 删除缓存
- shutdown(): 关闭缓存

### 缓存实现

1. **CacheGuavaImpl**: 基于 Guava Cache 的本地缓存
2. **CacheRedisImpl**: 基于 Redis 的分布式缓存

**缓存配置** (config/cache.json):
```json
{
  "type": "guava", // 或 redis
  "guavaMaximumSize": 1000,
  "guavaExpireSeconds": 600,
  "redisHost": "127.0.0.1",
  "redisPort": 6379,
  "redisPassword": "",
  "redisExpireSeconds": 600
}
```

---

## 配置系统

### Config

**路径**: `com.x.base.core.project.config.Config`

统一配置管理类。

### 配置类

| 配置类 | 说明 |
|-----|------|
| ApplicationServers | 应用服务器配置 |
| CenterServers | 中心服务器配置 |
| DataServers | 数据服务器配置 |
| StorageServers | 存储服务器配置 |
| WebServers | Web 服务器配置 |
| Token | Token 配置 |
| LogLevel | 日志级别 |
| Cms | CMS 配置 |
| ProcessPlatform | 流程平台配置 |
| Portal | 门户配置 |
| Query | 查询配置 |
| ExternalDataSources | 外部数据源配置 |
| ExternalStorageSources | 外部存储源配置 |
| Dingding | 钉钉集成配置 |
| WeLink | WeLink 集成配置 |
| ZhengwuDingding | 政务钉钉配置 |
| Mpweixin | 微信公众号配置 |
| Exmail | 腾讯企业邮配置 |
| Collect | 数据采集配置 |
| General | 通用配置 |
| WorkTime | 工作时间配置 |
| TernaryManagement | 三方管理配置 |

### 配置示例

**节点配置** (config/node_127.0.0.1.json):
```json
{
  "isPrimaryCenter": true,
  "application": {
    "enable": true,
    "port": 20020,
    "sslEnable": false
  },
  "data": {
    "enable": true,
    "port": 20050,
    "includes": [],
    "excludes": []
  },
  "storage": {
    "enable": true,
    "port": 20040,
    "name": "default"
  },
  "web": {
    "enable": true,
    "port": 80,
    "sslEnable": false
  }
}
```

---

## 脚本引擎

### GraalvmScriptingFactory

**路径**: `com.x.base.core.project.scripting.GraalvmScriptingFactory`

基于 GraalVM 的脚本执行引擎。

**支持的脚本语言**:
- JavaScript
- Python (可选)
- Ruby (可选)
- R (可选)

### 脚本上下文

在脚本中可访问的对象：

| 对象 | 说明 |
|-----|------|
| organization | 组织服务 |
| processPlatform | 流程服务 |
| cms | CMS 服务 |
| query | 查询服务 |
| file | 文件服务 |
| meeting | 会议服务 |
| calendar | 日程服务 |
| bbs | 论坛服务 |
| hotpic | 热点图片服务 |
| component | 组件服务 |
| attendance | 考勤服务 |
| jpush | 推送服务 |
| message | 消息服务 |
| mind | 脑图服务 |
| correlation | 关联服务 |
| programCenter | 程序中心服务 |
| ai | AI 服务 |
| context | 上下文 |
| request | 请求对象 |
| response | 响应对象 |
| log | 日志对象 |

### 脚本执行示例

```java
String script = "return 'Hello, ' + name;";
Map<String, Object> bindings = new HashMap<>();
bindings.put("name", "O2OA");
Object result = GraalvmScriptingFactory.eval(script, bindings);
```

---

## 工具类库

### BaseTools

**路径**: `com.x.base.core.project.tools.BaseTools`

基础工具类。

### 其他工具类

| 工具类 | 功能 |
|-----|------|
| DateTools | 日期工具 |
| StringTools | 字符串工具 |
| ListTools | 列表工具 |
| ClassLoaderTools | 类加载工具 |
| FileTools | 文件工具 |
| CompressTools | 压缩工具 |
| Crypto | 加密工具 |
| ImageTools | 图片工具 |
| DomainTools | 域名工具 |
| MD5Tool | MD5 工具 |
| Host | 主机信息 |
| DefaultCharset | 默认字符集 |
| LuceneTools | Lucene 工具 |
| LanguageTools | 语言工具 |
| LdapTools | LDAP 工具 |
| DocumentTools | 文档工具 |
| ExtractTextTools | 文本提取工具 |

---

## 异常体系

### Exception 基类

**路径**: `com.x.base.core.project.exception.*`

**主要异常类**:
- `PromptException`: 提示异常
- `LanguagePromptException`: 多语言提示异常
- `ExceptionAccessDenied`: 访问拒绝
- `ExceptionEntityExist`: 实体已存在
- `ExceptionEntityNotExist`: 实体不存在
- `ExceptionEntityFieldEmpty`: 字段为空
- `ExceptionPersonNotExist`: 人员不存在
- `ExceptionUnauthorized`: 未授权
- `ExceptionUnlawfulAddress`: 非法地址
- `ExceptionUnsupportedMediaType`: 不支持的媒体类型
- `ExceptionWhen`: 条件异常
- `ExceptionDeprecatedAction`: 已废弃的操作
- `ExceptionDuplicateFlag`: 重复标识
- `ExceptionDuplicateRestrictFlag`: 重复约束标识
- `ExceptionStorageMappingNotExist`: 存储映射不存在

---

## 日志系统

### Logger

**路径**: `com.x.base.core.project.logger.Logger`

日志封装类。

### LoggerFactory

**路径**: `com.x.base.core.project.logger.LoggerFactory`

日志工厂类。

### 日志配置

**路径**: `commons/log4j2.xml`

---

## HTTP 客户端

### HttpConnection

**路径**: `com.x.base.core.project.connection.HttpConnection`

HTTP 连接工具类。

**主要方法**:
- getAsString(): GET 请求获取字符串
- getAsBytes(): GET 请求获取字节
- postAsBytes(): POST 请求
- putAsBytes(): PUT 请求
- delete(): DELETE 请求
- upload(): 文件上传

### ActionResponse

HTTP 响应包装类。

---

## JAX-RS/REST 支持

### AbstractActionApplication

**路径**: `com.x.base.core.project.jaxrs.AbstractActionApplication`

REST 应用基类。

### StandardJaxrsAction

标准 JAX-RS 动作基类。

### 过滤器

| 过滤器 | 功能 |
|-----|------|
| TokenFilter | Token 验证 |
| UserJaxrsFilter | 用户过滤 |
| ManagerUserJaxrsFilter | 管理员过滤 |
| AnonymousJaxrsFilter | 匿名访问过滤 |
| CacheJaxrsFilter | 缓存过滤 |
| ApiAccessFilter | API 访问过滤 |
| OpenApiJaxrsFilter | 开放 API 过滤 |
| SysResourceJaxrsFilter | 系统资源过滤 |
| DescribeFilter | API 描述过滤 |
| DenialOfServiceFilter | DoS 防护过滤 |
| EchoJaxrsFilter | 回显过滤 |
| DruidFilter | Druid 监控过滤 |

### 响应包装类

| 类 | 说明 |
|-----|------|
| ActionResult | 动作结果 |
| WrapOutString | 字符串输出 |
| WrapOutStringList | 字符串列表输出 |
| WrapOutInteger | 整数输出 |
| WrapOutBoolean | 布尔输出 |
| WrapOutId | ID 输出 |
| WrapOutCount | 计数输出 |
| WrapOutMap | Map 输出 |
| WrapOutOnline | 在线信息输出 |
| WrapInString | 字符串输入 |
| WrapInStringList | 字符串列表输入 |

---

## 构建工具

### MetaModelBuilder

JPA 元模型构建器。

### EnhanceBaseBuilder

JPA 实体增强构建器。

### EnhanceBuilder

实体增强构建器。

### PersistenceXmlWriter

persistence.xml 生成器。

### SqlWriter

SQL 脚本生成器。

### TableBuilder

表构建工具。

### ApiBuilder

API 文档构建器。

### DescribeBuilder

描述文档构建器。

### CreateWebXml

web.xml 生成器。

### PackageO2server

O2Server 打包工具。

---

## 上下文和应用

### Context

**路径**: `com.x.base.core.project.Context`

应用上下文。

### Application

**路径**: `com.x.base.core.project.Application`

应用信息类。

### Applications

应用集合类。

---

## 消息通信

### MessageConnector

**路径**: `com.x.base.core.project.message.MessageConnector`

消息连接器。

**消息类型**:
- 待办消息
- 待阅消息
- 会议消息
- 日程消息
- 系统消息
- 自定义消息

---

## 监控和审计

### AuditLog

审计日志注解。

### Describe 系列注解

| 注解 | 说明 |
|-----|------|
| @Describe | 描述 |
| @FieldDescribe | 字段描述 |
| @FieldTypeDescribe | 字段类型描述 |
| @Module | 模块 |
| @ModuleCategory | 模块分类 |
| @ModuleType | 模块类型 |
| @JaxrsDescribe | JAX-RS 描述 |
| @JaxrsMethodDescribe | JAX-RS 方法描述 |
| @JaxrsParameterDescribe | JAX-RS 参数描述 |

---

## 存储系统

### Storage

**路径**: `com.x.base.core.entity.Storage`

存储配置类。

### StorageObject

存储对象基类。

### StorageType

存储类型枚举。

**支持的存储类型**:
- 文件系统
- FTP/SFTP
- 阿里云 OSS
- 腾讯云 COS
- AWS S3
- MinIO

### 存储配置 (config/externalStorageSources.json)

```json
{
  "default": "file",
  "storages": [
    {
      "name": "file",
      "protocol": "file",
      "host": "",
      "port": 0,
      "username": "",
      "password": "",
      "path": "/o2oa/storage"
    },
    {
      "name": "oss",
      "protocol": "aliyun_oss",
      "endpoint": "",
      "accessKeyId": "",
      "accessKeySecret": "",
      "bucket": ""
    }
  ]
}
```

---

## 定时任务

### AbstractJob

**路径**: `com.x.base.core.project.schedule.AbstractJob`

定时任务基类。

### ScheduleRequest

定时任务请求。

### ScheduleLocalRequest

本地定时任务请求。

---

## 依赖关系

其他所有模块都依赖 x_base_core_project：

```xml
<dependency>
  <groupId>net.o2oa</groupId>
  <artifactId>x_base_core_project</artifactId>
  <version>10.0</version>
</dependency>
```

---

## 扩展点

1. **自定义存储**: 实现新的 StorageProtocol
2. **自定义缓存**: 实现 Cache 接口
3. **自定义脚本函数**: 在脚本中注册自定义函数
4. **自定义过滤器**: 继承 JAX-RS Filter
5. **自定义工具**: 扩展 Tools 类

---

## 总结

x_base_core_project 是 O2OA 的心脏，提供了整个平台运行所需的基础设施。理解这个模块是进行 O2OA 开发的基础。

所有业务模块都依赖于这个核心模块，通过它提供的统一接口和工具类进行开发。
